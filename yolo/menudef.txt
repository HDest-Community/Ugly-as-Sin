AddOptionMenu "UaS_LoadedModules" {
	StaticText "YOLO"
}

// if bundled: AddOptionMenu "UaS_ConfigureModules" {
// if bundled: 	Submenu "YOLO", "UaS_YOLO_Options"
// if bundled: }

// if bundled: OptionMenu "UaS_YOLO_Options" {
// if bundled: 	Title "YOLO Module Settings"
// if bundled: 	Option "Enabled", "UaS_YOLO", "OnOff"
// if bundled: }
